{% extends "::layout.html.twig" %}

{% block page_content %}
    {% for article in pagination %}
        <div class="row article">
            <div class="col-lg-12">
                <div>
                    <h3>{{ article.title }}</h3>
                </div>
                <div>
                    <h4>
                        <span class="label label-default">
                            {{ article.created | date('M d, Y') }}
                        </span>
                    </h4>
                    <a href="#" class="btn btn-primary btn-xs">Edit post</a>
                    <a href="#" class="btn btn-danger btn-xs">Delete post</a>
                </div>
                <div class="content">
                    {{ article.content | raw }}
                </div>
            </div>
        </div>
    {% else %}
    <div class="row">
        <div class="col-lg-12">
            <div>
                <h3>No posts found!</h3>
            </div>
        </div>
    </div>
    {% endfor %}

    <div class="navigation">
        {{ knp_pagination_render(pagination, 'BlogMainBundle:Common:pagination.html.twig') }}
    </div>
{% endblock %}
